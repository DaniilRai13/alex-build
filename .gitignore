# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Responsive WebP variants — derived from src/assets, rebuilt by
# scripts/generate-image-variants.mjs on every build.
src/assets/generated

# Build archives
*.rar
*.zip

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Telegram secrets (server-side) – nikdy necommitovat
public/telegram-config.php

# Supabase service key for the visit counter (server-side, see public/track.php).
# Generated on the hosting from GitHub secrets — never in git, never in the
# browser bundle.
public/analytics-config.php

# Portfolio downloaded from Supabase before each build by
# scripts/fetch-portfolio.mjs. Derived data: the admin app is the source of
# truth, and re-downloading is cheaper than carrying ~20 MB of photos here.
src/assets/portfolio-remote
src/data/portfolio/portfolio.remote.json
